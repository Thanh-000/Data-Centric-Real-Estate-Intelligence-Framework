name: data-contracts

on:
  workflow_dispatch:
  push:
    paths:
      - "configs/data_contracts/**"
      - "src/dc_reif/governance/**"
      - ".github/workflows/data-contracts.yml"

jobs:
  validate-contracts:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Validate data contract files
        run: |
          python - <<'PY'
          import json
          from pathlib import Path
          for path in [
              Path("configs/data_contracts/data_manifest.json"),
              Path("configs/data_contracts/schema_contract.json"),
          ]:
              payload = json.loads(path.read_text(encoding="utf-8"))
              print(path, sorted(payload.keys()))
          PY
